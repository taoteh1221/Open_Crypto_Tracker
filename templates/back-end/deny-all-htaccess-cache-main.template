deny from all

# Leaving only ui_upgrade_alert.dat accessible (workaround for odd json PHP array echoing bug)
# Add no directory listing logic too (if ever used)
<IfModule mod_rewrite.c>

SetEnvIf Request_URI "(.*)/ui_upgrade_alert\.(.*)" allowed_uri

Allow from env=allowed_uri

</IfModule>

